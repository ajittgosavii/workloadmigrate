streamlit>=1.30.0
pandas>=2.0.0
openpyxl>=3.1.0
plotly>=5.18.0
anthropic>=0.40.0
requests>=2.31.0
markdown>=3.5.0
reportlab>=4.0.0

# Optional: AWS auto-discovery (install manually if needed)
# boto3>=1.34.0

# Optional: Azure auto-discovery (install manually if needed)
# azure-identity>=1.15.0
# azure-mgmt-compute>=30.0.0
